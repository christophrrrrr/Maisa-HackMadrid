# Vision extraction for the 29 scanned invoices.
# Preferred: free Google Gemini (AI Studio). Create a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=
# Optional model override (default: gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# Alternative route: Vercel AI Gateway (used only if GOOGLE_API_KEY is unset)
# AI_GATEWAY_API_KEY=
